#import <AppKit/AppKit.h>
#include <native.h>
#include "globals.h"

namespace native {

// No need to implement position(), size(), bounds() — use x(), y(), width(), height()

} // namespace native
